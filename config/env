#!/bin/bash

# flowup shell setup
# affix colons on either side of $PATH to simplify matching
case ":${PATH}:" in
    *:"$HOME/.flowx/bin":*)
        ;;
    *)
        export PATH="$HOME/.flowx/bin:$PATH"
        ;;
esac
